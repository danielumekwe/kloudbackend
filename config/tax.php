<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Tax rate
    |--------------------------------------------------------------------------
    |
    | A single flat percentage applied to every invoice's subtotal, admin-
    | editable via /admin/billing-settings — see PricingConfig::taxRatePercent().
    |
    */

    'rate_percent' => 0,

];
